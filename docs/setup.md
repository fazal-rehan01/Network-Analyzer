# Setup

> Populated across milestones as tooling is confirmed. Covers Windows + Linux/WSL, TShark, Zeek, Docker, env vars, database.

## Current environment (verified M1)

- Python 3.12.0 ✓
- Node 24.11.0, npm 11.19.0 ✓
- Git 2.55.0 ✓
- TShark (Wireshark) 4.6.7 ✓ at `C:\Program Files\Wireshark\tshark.exe`
- Zeek — not installed (optional feature)
- Docker — installed, daemon not running (optional)
